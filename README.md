## Project Setup Intructions.
* set DB credentials in .env file also convert .env.example into .env
* import the siliifjq_pjps_updated.sql in DB
* delete the composer.lock if it is exist.

## Run the following commnads.
* composer install
* php artisan migrate
* php artisan db:seed
